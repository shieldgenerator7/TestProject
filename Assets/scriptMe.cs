using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scriptMe : MonoBeshaviour
{
    // Start is called before the first frame update
    void Strt()
    {
        int i = 0;
        
    }

    // Update is called once per frame
    {
        
    }
}
